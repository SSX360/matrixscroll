INIT Init
NEXT Next

INVARIANT Inv_TypeOK
INVARIANT Inv_OrgNeverBelowEntitlement
INVARIANT Inv_ScopesMatchPlan

\* F-O2 constrains the sync step, not a single state — see OrgPlanSync.tla.
PROPERTY Prop_OrgMonotonic

\* A .cfg cannot hold a record literal, so bind the plan lattice by definition
\* override instead of by value.
CONSTANT Plans <- PlansDef
CONSTANT PlanRank <- PlanRankDef
