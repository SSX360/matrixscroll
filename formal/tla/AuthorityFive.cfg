INIT Init
NEXT Next

\* actionLog is append-only, so bound the finite model.
CONSTRAINT StateConstraint

INVARIANT Inv_TypeOK
INVARIANT Inv_NoPaymentWithoutPurchaseContext
INVARIANT Inv_SpendWithinMax

\* F-A1..F-A5 govern the step in which an action is taken — see AuthorityFive.tla.
PROPERTY Prop_NoPurchaseWithoutGrant
PROPERTY Prop_NoPaymentWithoutPaymentGrant
PROPERTY Prop_NoSubstitutionWithoutGrant
PROPERTY Prop_NoRenewalWithoutGrant
PROPERTY Prop_SearchNeverImpliesPurchase

\* Escalation actions must be unreachable -- model checker should report no violations
\* when these escalation transitions are disabled in production policy layer.

CONSTANT MaxSpend = 3
